package qt:plugin;

/// 窗口控制：宿主向插件开放的窗口调整能力
///
/// 插件可按自身内容需求调整工具窗口的尺寸（逻辑像素）；
/// 宿主在事件处理返回后应用该请求。
interface window {
    /// 请求调整插件窗口尺寸
    ///
    /// `width` / `height` 为逻辑像素。请求在当前事件（`dispatch-event`）
    /// 返回后由宿主应用，同一次事件内多次调用以最后一次为准。
    set-size: func(width: u32, height: u32);
}
