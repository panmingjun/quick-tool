[package]
name = "qt-runtime"
version.workspace = true
edition.workspace = true

[lints]
workspace = true

[dependencies]
qt-core.workspace = true
qt-sdk.workspace = true
# 插件独立键值存储（storage import 的宿主实现）
qt-storage.workspace = true

# WASM Runtime
wasmtime.workspace = true
wasmtime-wasi.workspace = true
wit-component.workspace = true

# Async
tokio.workspace = true

# Serialization
serde_json.workspace = true

# HTTP Client（可选）
reqwest = { workspace = true, optional = true }

# Error handling
thiserror.workspace = true
anyhow.workspace = true

# Logging
tracing.workspace = true

[features]
default = []
http = ["reqwest"]

[dev-dependencies]
slint-interpreter = { version = "=1.15.1", features = ["internal"] }
spin_on = "0.1.1"